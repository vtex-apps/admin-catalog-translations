query startSkuTranslations($locale: String!, $categoryId: String!) {
  skuTranslations(locale: $locale, categoryId: $categoryId)
    @context(provider: "vtex.admin-catalog-translation") {
    requestId
  }
}

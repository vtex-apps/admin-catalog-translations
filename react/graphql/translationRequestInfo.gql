query translationRequestInfo($requestId: String!, $bucket: DownloadBucket!) {
  translationRequestInfo(requestId: $requestId, bucket: $bucket) {
    requestId
    requestedBy
    categoryId
    error
    createdAt
    locale
    completedAt
    estimatedTime
  }
}
